	AREA PALINDROME, CODE, READONLY
N RN 1
RE RN 2
DS RN 3
LD RN 4
RES RN 5
Q RN 6
DV RN 7
ENTRY
	MOV N, #101
	MOV RE, #0
	MOV DS, #10
	MOV LD, #0
	MOV RES, #0
	MOV DV, N
LOOP
	BL DIV
	MUL RE, DS, RE
	ADD RE, RE, LD
	CMP Q, #0
	MOV DV, Q
	BNE LOOP
	CMP RE, N
	MOVPL RES, #1
	MOVMI RES, #0
GO B GO
DIV
	MOV Q, #0
LOOP2
	SUBS DV, DV, DS
	ADDPL Q, Q, #1
	BPL LOOP2
	ADDMI LD, DV, DS
	MOV PC, LR
	END
